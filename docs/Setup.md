# Setup Guide v2.0.0

## Prerequisites
- Python 3.11+
- Virtual Environment

## Initialization
1. Ensure `requirements.txt` is installed.
2. Run `uvicorn api.main:app --reload` to initialize the SQLAlchemy ORM schema.
3. Use the default `admin` credentials provided by the initialization script to log in.
